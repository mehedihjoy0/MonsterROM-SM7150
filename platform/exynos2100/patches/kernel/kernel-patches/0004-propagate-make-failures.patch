--- a/build/scripts/build.sh
+++ b/build/scripts/build.sh
@@ -42,6 +42,7 @@ build() {
             make "$@" >> log.txt 2>&1
         else
             make "$@" 2>&1 | tee -a log.txt
+            return "${PIPESTATUS[0]}"
         fi
     }
 
